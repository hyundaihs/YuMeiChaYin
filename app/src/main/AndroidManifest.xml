<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sp.shangpin">

    <!--网络权限-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.LoginActivity" />

        <activity android:name=".ui.RegisterActivity" />

        <activity android:name=".ui.HomeActivity" />

        <activity android:name=".ui.GoodsDetailsActivity" />

        <activity android:name=".ui.TopUpActivity" />

        <activity android:name=".ui.LotteryActivity" />

        <activity android:name=".ui.RuleActivity" />
        <activity android:name=".ui.OrdersActivity" />
        <activity android:name=".ui.InputAddrActivity" />
        <activity android:name=".ui.NormalGoodsActivity" />
        <activity android:name=".ui.NormalGoodsDetailsActivity" />
        <activity android:name=".ui.NormalOrdersActivity" />
        <activity android:name=".ui.YhqActivity" />
        <activity android:name=".ui.AlertPasswordActivity" />
    </application>

</manifest>